<?php include '../includes/header.php'; ?>
<?php include '../includes/navbar.php'; ?>

<!-- Animated Background Elements -->
<div class="vintage-border"></div>
<div class="tamil-pattern"></div>

<!-- Floating Tamil decorative elements -->
<div class="floating-element floating-tamil-1">அ</div>
<div class="floating-element floating-tamil-2">ண்</div>
<div class="floating-element floating-tamil-3">ணா</div>

<!-- Corner decorations -->
<div class="corner-decoration corner-top-left"></div>
<div class="corner-decoration corner-top-right"></div>
<div class="corner-decoration corner-bottom-left"></div>
<div class="corner-decoration corner-bottom-right"></div>

<div class="container">
<h1>பேரறிஞர் அண்ணாதுரை – வாழ்க்கை, நாவன்மை, அரசியல்</h1>

<div class="story-content">

<section class="story-section" data-category="introduction">
  <p>
    காஞ்சீவரம் நடராசன் அண்ணாதுரை, இந்திய அரசியல்வாதியும்,
    மதராஸ் மாநிலத்தின் கடைசி முதல்வரும்,
    தமிழ்நாட்டின் முதலாவது முதலமைச்சருமாவார்.
    அவர் "அறிஞர் அண்ணா", "பேரறிஞர் அண்ணா" எனப் போற்றப்படுகிறார்.
  </p>
</section>

<section class="story-section" data-category="early-life">
  <p>
    சின்ன காஞ்சீபுரத்தில் வரகுவாசல் தெருவில்,
    54ஆம் கதவெண் கொண்ட வீட்டில்,
    செங்குந்தக் கைக்கோள முதலியார் மரபைச் சேர்ந்த
    நடராசன் முதலியார் – பங்காரு அம்மாள் தம்பதியருக்கு,
    1909 ஆம் ஆண்டு செப்டம்பர் 15 ஆம் தேதி,
    செளமிய ஆண்டு ஆவணித்திங்கள் 31 ஆம் நாள் பிறந்தார்.
  </p>
</section>

<section class="story-section" data-category="family">
  <p>
    அண்ணா சிறு வயதில் தாயை இழந்தார்.
    அதன் பின்னர் தந்தை நடராசன்,
    இராசாமணி என்பவரை மறுமணம் செய்து கொண்டார்.
    அண்ணா அவரை "தொத்தா" என்று அன்புடன் அழைத்தார்.
  </p>
</section>

<section class="story-section" data-category="education">
  <p>
    1934 இல் இளங்கலை (ஆனர்ஸ்) பட்டம்,
    அதனைத் தொடர்ந்து பொருளியல், அரசியல்
    முதுகலைப் பட்டங்களை
    சென்னை பச்சையப்பன் கல்லூரியில் பெற்றார்.
    பின்னர் அதே கல்லூரியில்
    ஆங்கில ஆசிரியராகப் பணியாற்றினார்.
  </p>
</section>

<section class="story-section" data-category="language">
  <p>
    ஆங்கில மொழியில் வல்லவராக இருந்தபோதிலும்,
    தேவையற்ற இடங்களில் ஆங்கிலம் பேசுவதை
    அண்ணா விரும்பவில்லை.
    ஒருமுறை பாட்டி கேட்டபோதும்,
    போலி ஆங்கிலத்தைத் தவிர்த்தார்.
  </p>
</section>

<section class="story-section" data-category="oratory">
  <p>
    ஆங்கிலத்திலும் தமிழிலும்
    அற்புதமான சொற்பொழிவாற்றும் திறன் பெற்றவர்.
    பல சீர்திருத்த நாடகங்களையும்
    சமூக கருத்துகளை மையமாகக் கொண்டு எழுதியவர்.
  </p>
</section>

<section class="story-section" data-category="anecdote">
  <p>
    ஒருமுறை,
    இங்கிலாந்து மாணவர்கள் சிலர்,
    "because" என்ற சொல்லை
    மூன்று முறை தொடர்ந்து வரும் வாக்கியம்
    சொல்ல முடியுமா எனக் கேட்டனர்.
    அண்ணா உடனே பதிலளித்தார்:
  </p>
</section>

<section class="story-section highlight-box" data-category="quote">
  <p class="quote-text">
    "No sentence can end with because,
    because, because is a conjunction."
  </p>
</section>

<section class="story-section" data-category="politics">
  <p>
    பெரியார் கொள்கைகளால் ஈர்க்கப்பட்டு,
    நீதிக்கட்சியில் சேர்ந்தார்.
    பின்னர் திராவிடர் கழகத்தில்
    சமூக சீர்திருத்தக் கருத்துகளை
    பரவலாக எடுத்துச் சென்றார்.
  </p>
</section>

<section class="story-section" data-category="founding">
  <p>
    1949 ஆம் ஆண்டு,
    பெரியாருடன் ஏற்பட்ட கருத்து வேறுபாட்டின் காரணமாக,
    திராவிட முன்னேற்றக் கழகம் (திமுக)
    என்னும் புதிய இயக்கத்தை
    அண்ணா நிறுவினார்.
  </p>
</section>

<section class="story-section highlight-box" data-category="slogan">
  <p class="highlight-text">
    அண்ணா முன்மொழிந்த
    முக்கிய முழக்கம்:
    <span class="bold-slogan">"கடமை, கண்ணியம், கட்டுப்பாடு"</span>.
  </p>
</section>

<section class="story-section" data-category="achievement">
  <p>
    1967 ஆம் ஆண்டு நடைபெற்ற தேர்தலில்,
    திமுக வெற்றி பெற்று,
    தமிழ்நாட்டில்
    முதல் முறையாக திராவிட ஆட்சி அமைந்தது.
    மார்ச் 6, 1967 அன்று
    அண்ணா முதலமைச்சராகப் பொறுப்பேற்றார்.
  </p>
</section>

<section class="story-section" data-category="reforms">
  <p>
    சுயமரியாதைத் திருமணங்களை
    சட்டபூர்வமாக்கினார்.
    இரு மொழிக் கொள்கையை
    தமிழ் – ஆங்கிலம் என நடைமுறைப்படுத்தினார்.
  </p>
</section>

<section class="story-section" data-category="legacy">
  <p>
    "மதராஸ் மாநிலம்" என்ற பெயரை
    1969 ஜனவரி 14 அன்று
    "தமிழ்நாடு" என மாற்றி,
    வரலாற்றில் நிலையான இடம் பெற்றார்.
  </p>
</section>

<section class="story-section" data-category="honors">
  <p>
    யேல் பல்கலைக்கழகம்
    1967–1968 இல்
    அண்ணாவுக்கு
    கவுரவ பேராசிரியர் பட்டம் வழங்கியது.
    அமெரிக்கர் அல்லாதவருக்கு
    அந்த விருது கிடைத்தது
    அதுவே முதல் முறை.
  </p>
</section>

<section class="story-section" data-category="demise">
  <p>
    புற்றுநோயால் பாதிக்கப்பட்ட அண்ணா,
    1969 பிப்ரவரி 3 அன்று
    உயிர்நீத்தார்.
    அவரது இறுதி ஊர்வலம்
    கின்னஸ் உலகச் சாதனையில்
    இடம் பெற்றது.
  </p>
</section>

<section class="story-section" data-category="works">
  <div class="works-grid">
    <div class="work-item">
      <span class="work-icon">🎭</span>
      <span class="work-title">நாடகங்கள்</span>
      <span class="work-count">13</span>
    </div>
    <div class="work-item">
      <span class="work-icon">🎬</span>
      <span class="work-title">திரைப்பட வசனங்கள்</span>
      <span class="work-count">10</span>
    </div>
    <div class="work-item">
      <span class="work-icon">📜</span>
      <span class="work-title">கவிதைகள்</span>
      <span class="work-count">77</span>
    </div>
    <div class="work-item">
      <span class="work-icon">📝</span>
      <span class="work-title">ஆங்கிலக் கட்டுரைகள்</span>
      <span class="work-count">129</span>
    </div>
    <div class="work-item">
      <span class="work-icon">📖</span>
      <span class="work-title">சிறுகதைகள்</span>
      <span class="work-count">113</span>
    </div>
    <div class="work-item">
      <span class="work-icon">📚</span>
      <span class="work-title">குறுநாவல்கள்</span>
      <span class="work-count">24</span>
    </div>
    <div class="work-item">
      <span class="work-icon">✍️</span>
      <span class="work-title">தமிழ்க் கட்டுரைகள்</span>
      <span class="work-count">3,476</span>
    </div>
    <div class="work-item">
      <span class="work-icon">🗣️</span>
      <span class="work-title">சொற்பொழிவுகள்</span>
      <span class="work-count">12,775</span>
    </div>
  </div>
</section>

<section class="story-section" data-category="quotes">
  <h3 class="quotes-heading">புகழ்பெற்ற சொற்றொடர்கள்</h3>
  <div class="quotes-grid">
    <div class="quote-item">
      <span class="quote-icon">💭</span>
      <span>"மறப்போம்… மன்னிப்போம்…"</span>
    </div>
    <div class="quote-item">
      <span class="quote-icon">⚔️</span>
      <span>"கத்தியைத் தீட்டாதே… புத்தியைத் தீட்டு"</span>
    </div>
    <div class="quote-item">
      <span class="quote-icon">🙏</span>
      <span>"ஏழையின் சிரிப்பில் இறைவனைக் காண்போம்"</span>
    </div>
    <div class="quote-item">
      <span class="quote-icon">⚖️</span>
      <span>"சட்டம் ஓர் இருட்டறை…"</span>
    </div>
  </div>
</section>

<section class="story-section" data-category="conclusion">
  <p class="conclusion-text">
    தமிழ், சமூக நீதி,
    மொழி உரிமை,
    பகுத்தறிவு ஆகியவற்றின்
    மிகச் சிறந்த பிரதிநிதியாக,
    பேரறிஞர் அண்ணாதுரை
    இன்றளவும் தமிழர் மனங்களில்
    நிலைத்திருக்கிறார்.
  </p>
</section>

</div>
</div>

<style>
/* ===== CSS VARIABLES & THEME ===== */
:root {
  --parchment: #E6D6B8;
  --muted-beige: #D8C8A6;
  --light-cream: #EFE6CF;
  --olive-green: #6E7B5B;
  --forest-green: #3F4A3C;
  --moss-green: #8A946E;
  --charcoal: #2F2F2A;
  --faded-black: #1E1E1C;
  --dusty-brown: #7A6A55;
  --off-white: #F5F5F0;
  --soft-gold: #C2A86D;
  --muted-white: #ECECEC;
  --tamil-red: #b33a3a;
  --shadow: rgba(0, 0, 0, 0.3);
  --shadow-light: rgba(194, 168, 109, 0.1);
  --shadow-lg: rgba(0, 0, 0, 0.4);
  --radius: 12px;
  --radius-sm: 8px;
  --radius-lg: 16px;
  --transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ===== ANIMATIONS ===== */
@keyframes gradientFlow {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

@keyframes grainEffect {
  0%, 100% { transform: translate(0, 0); }
  10% { transform: translate(-1%, -1%); }
  20% { transform: translate(2%, 0%); }
  30% { transform: translate(-1%, 2%); }
  40% { transform: translate(1%, -1%); }
  50% { transform: translate(-2%, 1%); }
  60% { transform: translate(1%, 2%); }
  70% { transform: translate(2%, -1%); }
  80% { transform: translate(-1%, 1%); }
  90% { transform: translate(1%, -2%); }
}

@keyframes floatParticles {
  0% { transform: translateY(0) rotate(0deg); opacity: 0.7; }
  50% { transform: translateY(-20px) rotate(180deg); opacity: 0.9; }
  100% { transform: translateY(0) rotate(360deg); opacity: 0.7; }
}

@keyframes shimmer {
  0% { background-position: -1000px 0; }
  100% { background-position: 1000px 0; }
}

@keyframes twinkle {
  0%, 100% { opacity: 0.1; }
  50% { opacity: 0.4; }
}

@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes fadeInParagraph {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes titleGlow {
  0% {
    text-shadow: 2px 2px 3px rgba(0, 0, 0, 0.1),
                 0 0 20px rgba(110, 123, 91, 0.2);
  }
  100% {
    text-shadow: 2px 2px 8px rgba(110, 123, 91, 0.4),
                 0 0 30px rgba(194, 168, 109, 0.3);
  }
}

@keyframes floatSlow {
  0% {
    transform: translate(0, 0) rotate(0deg) scale(1);
  }
  25% {
    transform: translate(20px, 30px) rotate(5deg) scale(1.1);
  }
  50% {
    transform: translate(0, 60px) rotate(0deg) scale(1);
  }
  75% {
    transform: translate(-20px, 30px) rotate(-5deg) scale(1.1);
  }
  100% {
    transform: translate(0, 0) rotate(0deg) scale(1);
  }
}

@keyframes floatSlowReverse {
  0% {
    transform: translate(0, 0) rotate(0deg) scale(1);
  }
  25% {
    transform: translate(-30px, 20px) rotate(-5deg) scale(1.1);
  }
  50% {
    transform: translate(0, 40px) rotate(0deg) scale(1);
  }
  75% {
    transform: translate(30px, 20px) rotate(5deg) scale(1.1);
  }
  100% {
    transform: translate(0, 0) rotate(0deg) scale(1);
  }
}

@keyframes quotePulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.02); }
}

@keyframes sectionReveal {
  from {
    clip-path: inset(0 100% 0 0);
    opacity: 0;
  }
  to {
    clip-path: inset(0 0 0 0);
    opacity: 1;
  }
}

/* ===== BASE STYLES ===== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
  -ms-text-size-adjust: 100%;
  scroll-behavior: smooth;
}

body {
  background: 
    linear-gradient(-45deg, var(--charcoal), var(--faded-black), var(--forest-green), var(--dusty-brown));
  background-size: 400% 400%;
  animation: gradientFlow 20s ease infinite;
  color: var(--off-white);
  font-family: 'Georgia', 'serif', 'Latha', 'Tamil Sangam MN', 'Noto Sans Tamil', serif;
  line-height: 1.6;
  overflow-x: hidden;
  position: relative;
  min-height: 100vh;
}

/* Animated Grain Overlay */
body::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 400 400' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%' height='100%' filter='url(%23noiseFilter)' opacity='0.1'/%3E%3C/svg%3E");
  opacity: 0.15;
  animation: grainEffect 8s steps(10) infinite;
  pointer-events: none;
  z-index: 1;
}

/* Floating Particles */
.floating-particle {
  position: fixed;
  width: 4px;
  height: 4px;
  background-color: var(--soft-gold);
  border-radius: 50%;
  pointer-events: none;
  opacity: 0.7;
  animation: floatParticles 20s infinite linear;
  z-index: 0;
}

.container {
  max-width: 900px;
  margin: clamp(40px, 6vw, 60px) auto;
  background-color: var(--parchment);
  position: relative;
  box-shadow: 
    0 0 40px rgba(0, 0, 0, 0.5),
    0 0 100px rgba(194, 168, 109, 0.1) inset;
  border: 1px solid var(--dusty-brown);
  border-radius: var(--radius-lg);
  padding: clamp(30px, 4vw, 40px);
  animation: fadeInUp 1.2s ease-out;
  z-index: 2;
  position: relative;
  width: 90%;
}

/* Shimmer Effect on Container */
.container::after {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.1),
    transparent
  );
  animation: shimmer 8s infinite;
  z-index: 1;
}

/* Pseudo-elements for enhanced vintage paper effect */
.container::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 100%;
  background: 
    linear-gradient(90deg, transparent 0%, rgba(106, 90, 66, 0.08) 50%, transparent 100%),
    linear-gradient(rgba(106, 90, 66, 0.1) 1px, transparent 1px);
  background-size: 100% 100%, 100% 2em;
  pointer-events: none;
  z-index: -1;
}

.container > * {
  position: relative;
  z-index: 2;
}

/* Tamil-inspired header with enhanced styling */
h1 {
  text-align: center;
  color: var(--forest-green);
  font-size: clamp(1.8rem, 4vw, 2.8rem);
  margin: clamp(20px, 3vw, 30px) 0;
  padding-bottom: clamp(12px, 2vw, 15px);
  border-bottom: 3px double var(--soft-gold);
  font-family: 'Kavivanar', 'Latha', 'Georgia', 'Noto Sans Tamil', serif;
  position: relative;
  text-shadow: 2px 2px 3px rgba(0, 0, 0, 0.1);
  animation: titleGlow 3s ease-in-out infinite alternate;
  background: linear-gradient(90deg, var(--forest-green), var(--olive-green), var(--forest-green));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1.4;
  word-wrap: break-word;
  overflow-wrap: break-word;
}

h1::before, h1::after {
  content: '✻';
  color: var(--soft-gold);
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  font-size: clamp(1.5rem, 3vw, 2rem);
  animation: twinkle 2s infinite;
}

h1::before {
  left: clamp(10px, 3vw, 20px);
  animation-delay: 0.5s;
}

h1::after {
  right: clamp(10px, 3vw, 20px);
  animation-delay: 1s;
}

/* ===== STORY CONTENT ===== */
.story-content {
  position: relative;
  font-size: clamp(1rem, 1.8vw, 1.15rem);
  color: var(--faded-black);
  line-height: 1.8;
}

/* Story Sections */
.story-section {
  margin-bottom: clamp(20px, 3vw, 25px);
  animation: fadeInParagraph 1s ease-out forwards;
  opacity: 0;
  transform: translateY(20px);
  position: relative;
  padding: clamp(15px, 2.5vw, 20px);
  border-radius: var(--radius);
  transition: var(--transition);
  border-left: 3px solid transparent;
}

/* Staggered animation delays */
.story-section:nth-child(1) { animation-delay: 0.2s; }
.story-section:nth-child(2) { animation-delay: 0.3s; }
.story-section:nth-child(3) { animation-delay: 0.4s; }
.story-section:nth-child(4) { animation-delay: 0.5s; }
.story-section:nth-child(5) { animation-delay: 0.6s; }
.story-section:nth-child(6) { animation-delay: 0.7s; }
.story-section:nth-child(7) { animation-delay: 0.8s; }
.story-section:nth-child(8) { animation-delay: 0.9s; }
.story-section:nth-child(9) { animation-delay: 1.0s; }
.story-section:nth-child(10) { animation-delay: 1.1s; }
.story-section:nth-child(11) { animation-delay: 1.2s; }
.story-section:nth-child(12) { animation-delay: 1.3s; }
.story-section:nth-child(13) { animation-delay: 1.4s; }
.story-section:nth-child(14) { animation-delay: 1.5s; }
.story-section:nth-child(15) { animation-delay: 1.6s; }
.story-section:nth-child(16) { animation-delay: 1.7s; }
.story-section:nth-child(17) { animation-delay: 1.8s; }
.story-section:nth-child(18) { animation-delay: 1.9s; }
.story-section:nth-child(19) { animation-delay: 2.0s; }
.story-section:nth-child(20) { animation-delay: 2.1s; }
.story-section:nth-child(21) { animation-delay: 2.2s; }

.story-section p {
  margin-bottom: 0;
}

.story-section:hover {
  background: linear-gradient(to right, transparent, var(--light-cream), transparent);
  border-left: 3px solid var(--moss-green);
  transform: translateX(5px);
  box-shadow: 2px 2px 15px rgba(0, 0, 0, 0.1);
}

/* Category-specific styling */
.story-section[data-category="introduction"] {
  border-left-color: var(--forest-green);
}

.story-section[data-category="quote"] {
  border-left-color: var(--soft-gold);
}

.story-section[data-category="slogan"] {
  border-left-color: var(--tamil-red);
}

/* Highlight Boxes */
.highlight-box {
  background: linear-gradient(145deg, 
    rgba(230, 214, 184, 0.3) 0%,
    rgba(212, 167, 98, 0.1) 100%
  );
  border: 2px solid var(--soft-gold);
  border-radius: var(--radius);
  padding: clamp(20px, 3vw, 25px);
  margin: clamp(25px, 4vw, 30px) 0;
  text-align: center;
  animation: quotePulse 3s ease-in-out infinite;
}

.quote-text {
  font-size: clamp(1.1rem, 2vw, 1.3rem);
  font-style: italic;
  color: var(--forest-green);
  font-weight: 600;
  line-height: 1.6;
  text-align: center;
  margin: 0;
  padding: 0;
  background: none;
  border-left: none;
}

.highlight-text {
  font-size: clamp(1.1rem, 2vw, 1.3rem);
  color: var(--faded-black);
  text-align: center;
  margin: 0;
}

.bold-slogan {
  color: var(--tamil-red);
  font-weight: 700;
  font-size: clamp(1.2rem, 2.5vw, 1.5rem);
  display: block;
  margin-top: 10px;
  text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.1);
}

/* ===== WORKS GRID ===== */
.works-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: clamp(12px, 2vw, 15px);
  margin: clamp(20px, 3vw, 25px) 0;
}

.work-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, 
    rgba(110, 123, 91, 0.1) 0%,
    rgba(138, 148, 110, 0.05) 100%
  );
  border: 1px solid var(--moss-green);
  border-radius: var(--radius-sm);
  padding: clamp(15px, 2vw, 20px);
  transition: var(--transition);
  text-align: center;
}

.work-item:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 20px rgba(110, 123, 91, 0.2);
  border-color: var(--soft-gold);
}

.work-icon {
  font-size: clamp(1.5rem, 3vw, 2rem);
  margin-bottom: 8px;
  color: var(--olive-green);
}

.work-title {
  font-size: clamp(0.8rem, 1.5vw, 0.9rem);
  color: var(--faded-black);
  font-weight: 600;
  margin-bottom: 5px;
}

.work-count {
  font-size: clamp(1rem, 2vw, 1.2rem);
  color: var(--forest-green);
  font-weight: 700;
}

/* ===== QUOTES GRID ===== */
.quotes-heading {
  text-align: center;
  color: var(--forest-green);
  font-size: clamp(1.2rem, 2.5vw, 1.5rem);
  margin: clamp(20px, 3vw, 25px) 0 clamp(15px, 2vw, 20px);
  font-family: 'Kavivanar', 'Noto Sans Tamil', serif;
}

.quotes-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: clamp(12px, 2vw, 15px);
  margin-bottom: clamp(20px, 3vw, 25px);
}

.quote-item {
  display: flex;
  align-items: center;
  gap: clamp(8px, 1.5vw, 12px);
  background: linear-gradient(to right, 
    transparent, 
    rgba(194, 168, 109, 0.05), 
    transparent
  );
  padding: clamp(12px, 2vw, 15px);
  border-radius: var(--radius-sm);
  border-left: 3px solid var(--olive-green);
  transition: var(--transition);
}

.quote-item:hover {
  background: linear-gradient(to right, 
    transparent, 
    rgba(194, 168, 109, 0.1), 
    transparent
  );
  border-left-color: var(--soft-gold);
  transform: translateX(5px);
}

.quote-icon {
  font-size: clamp(1.2rem, 2vw, 1.5rem);
  color: var(--olive-green);
  flex-shrink: 0;
}

.quote-item span:last-child {
  color: var(--faded-black);
  font-size: clamp(0.9rem, 1.8vw, 1rem);
  line-height: 1.5;
}

/* Conclusion Text */
.conclusion-text {
  text-align: center;
  font-size: clamp(1.1rem, 2vw, 1.3rem);
  color: var(--forest-green);
  font-weight: 600;
  padding: clamp(20px, 3vw, 25px);
  background: linear-gradient(to right, 
    transparent, 
    rgba(110, 123, 91, 0.05), 
    transparent
  );
  border-radius: var(--radius);
  margin-top: clamp(25px, 4vw, 30px);
  border: 1px solid rgba(110, 123, 91, 0.2);
}

/* ===== DECORATIVE ELEMENTS ===== */
.vintage-border {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  pointer-events: none;
  z-index: 1;
}

.vintage-border::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: clamp(10px, 2vw, 20px);
  background: linear-gradient(90deg, 
    transparent,
    var(--soft-gold),
    var(--olive-green),
    var(--soft-gold),
    transparent
  );
  opacity: 0.4;
  animation: shimmer 4s infinite;
}

.vintage-border::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: clamp(10px, 2vw, 20px);
  background: linear-gradient(90deg, 
    transparent,
    var(--soft-gold),
    var(--olive-green),
    var(--soft-gold),
    transparent
  );
  opacity: 0.4;
  animation: shimmer 4s infinite reverse;
}

.tamil-pattern {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url("data:image/svg+xml,%3Csvg width='120' height='120' viewBox='0 0 120 120' xmlns='http://www.w3.org/2000/svg'%3E%3Cdefs%3E%3Cpattern id='tamilPattern' x='0' y='0' width='120' height='120' patternUnits='userSpaceOnUse'%3E%3Cg fill='none' stroke='%236E7B5B' stroke-width='0.5' opacity='0.15'%3E%3Cpath d='M60 20 L80 40 L70 60 L50 50 L40 70 L20 60 L30 40 Z'/%3E%3Cpath d='M30 90 L50 110 L40 130 L20 120 L10 140 L-10 130 L0 110 Z'/%3E%3Cpath d='M90 90 L110 110 L100 130 L80 120 L70 140 L50 130 L60 110 Z'/%3E%3C/g%3E%3C/pattern%3E%3C/defs%3E%3Crect width='100%25' height='100%25' fill='url(%23tamilPattern)'/%3E%3C/svg%3E");
  opacity: 0.3;
  pointer-events: none;
  z-index: 0;
  animation: grainEffect 60s infinite linear;
}

.corner-decoration {
  position: absolute;
  width: clamp(50px, 10vw, 80px);
  height: clamp(50px, 10vw, 80px);
  z-index: 3;
  opacity: 0.8;
  border: 3px solid var(--soft-gold);
}

.corner-top-left {
  top: -5px;
  left: -5px;
  border-right: none;
  border-bottom: none;
  border-top-left-radius: 10px;
}

.corner-top-right {
  top: -5px;
  right: -5px;
  border-left: none;
  border-bottom: none;
  border-top-right-radius: 10px;
}

.corner-bottom-left {
  bottom: -5px;
  left: -5px;
  border-right: none;
  border-top: none;
  border-bottom-left-radius: 10px;
}

.corner-bottom-right {
  bottom: -5px;
  right: -5px;
  border-left: none;
  border-top: none;
  border-bottom-right-radius: 10px;
}

.floating-element {
  position: fixed;
  opacity: 0.08;
  z-index: 0;
  font-size: clamp(4rem, 10vw, 8rem);
  color: var(--olive-green);
  font-family: 'Kavivanar', 'Noto Sans Tamil', serif;
  pointer-events: none;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
  filter: drop-shadow(0 0 5px var(--soft-gold));
}

.floating-tamil-1 {
  top: 15%;
  left: 5%;
  animation: floatSlow 25s infinite linear;
}

.floating-tamil-2 {
  top: 60%;
  right: 8%;
  animation: floatSlowReverse 30s infinite linear;
}

.floating-tamil-3 {
  bottom: 20%;
  left: 10%;
  animation: floatSlow 35s infinite linear;
}

/* ===== RESPONSIVE DESIGN ===== */

/* Tablets and small desktops */
@media (max-width: 1024px) {
  .works-grid {
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }
  
  .quotes-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Tablets */
@media (max-width: 768px) {
  .container {
    margin: 30px auto;
    padding: 25px;
    width: 92%;
  }
  
  h1::before,
  h1::after {
    display: none;
  }
  
  .story-section {
    padding: 15px;
    margin-bottom: 15px;
  }
  
  .works-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 10px;
  }
  
  .quotes-grid {
    grid-template-columns: 1fr;
  }
  
  .floating-element {
    font-size: clamp(3rem, 8vw, 5rem);
  }
  
  .corner-decoration {
    width: 40px;
    height: 40px;
  }
}

/* Large smartphones */
@media (max-width: 576px) {
  body {
    padding: 0;
  }
  
  .container {
    width: 95%;
    margin: 20px auto;
    padding: 20px;
    border-radius: 20px;
  }
  
  h1 {
    font-size: 1.5rem;
    margin: 15px 0 20px;
    padding-bottom: 12px;
    -webkit-text-fill-color: var(--forest-green);
    background: none;
  }
  
  .story-content {
    font-size: 1rem;
    text-align: left;
  }
  
  .story-section {
    padding: 12px;
    margin-bottom: 12px;
  }
  
  .highlight-box {
    padding: 15px;
    margin: 20px 0;
  }
  
  .quote-text {
    font-size: 1rem;
  }
  
  .bold-slogan {
    font-size: 1.2rem;
  }
  
  .works-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 8px;
  }
  
  .work-item {
    padding: 12px;
  }
  
  .work-icon {
    font-size: 1.5rem;
  }
  
  .work-title {
    font-size: 0.8rem;
  }
  
  .work-count {
    font-size: 1rem;
  }
  
  .quotes-heading {
    font-size: 1.2rem;
    margin: 15px 0;
  }
  
  .quote-item {
    padding: 10px;
    gap: 8px;
  }
  
  .quote-icon {
    font-size: 1.2rem;
  }
  
  .conclusion-text {
    padding: 15px;
    margin-top: 20px;
    font-size: 1.1rem;
  }
  
  .floating-element {
    font-size: 2.5rem;
    opacity: 0.05;
  }
  
  .floating-tamil-1 {
    left: 2%;
  }
  
  .floating-tamil-2 {
    right: 3%;
  }
  
  .floating-tamil-3 {
    left: 5%;
  }
  
  /* Simplify hover effects on touch devices */
  @media (hover: none) {
    .story-section:hover {
      transform: none;
      box-shadow: none;
    }
    
    .work-item:hover {
      transform: none;
      box-shadow: none;
    }
    
    .quote-item:hover {
      transform: none;
    }
    
    .container:hover {
      box-shadow: 
        0 0 40px rgba(0, 0, 0, 0.5),
        0 0 100px rgba(194, 168, 109, 0.1) inset;
    }
  }
}

/* Extra small devices (phones in portrait) */
@media (max-width: 400px) {
  .container {
    padding: 15px;
    width: 98%;
  }
  
  h1 {
    font-size: 1.3rem;
  }
  
  .works-grid {
    grid-template-columns: 1fr;
  }
  
  .work-item {
    padding: 10px;
  }
  
  .quotes-grid {
    gap: 10px;
  }
  
  .quote-item {
    padding: 8px;
  }
  
  .floating-element {
    font-size: 2rem;
  }
}

/* Height-based adjustments for landscape mobile */
@media (max-height: 600px) and (orientation: landscape) {
  .container {
    margin: 20px auto;
    padding: 20px;
  }
  
  .story-section {
    padding: 10px;
    margin-bottom: 10px;
  }
  
  .works-grid {
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
  }
  
  .work-item {
    padding: 10px;
  }
  
  .work-title {
    font-size: 0.75rem;
  }
}

/* High-resolution displays */
@media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
  .story-section {
    border-left-width: 2px;
  }
  
  .quote-item {
    border-left-width: 2px;
  }
}

/* Reduced motion preference */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
  
  body::before,
  .vintage-border::before,
  .vintage-border::after,
  .tamil-pattern,
  .floating-element,
  .container::after,
  .floating-particle {
    animation: none !important;
  }
  
  .highlight-box {
    animation: none !important;
  }
  
  .story-section {
    animation: fadeIn 0.3s ease-out !important;
  }
}

/* Print-friendly styles */
@media print {
  .vintage-border, 
  .tamil-pattern, 
  .floating-element, 
  .corner-decoration,
  .floating-particle {
    display: none;
  }
  
  body {
    background: white !important;
    animation: none !important;
    color: black !important;
  }
  
  body::before {
    display: none;
  }
  
  .container {
    box-shadow: none !important;
    border: 1px solid #ddd !important;
    animation: none !important;
    width: 100% !important;
    max-width: 100% !important;
    margin: 0 !important;
    padding: 20px !important;
  }
  
  .container::after {
    animation: none;
    display: none;
  }
  
  h1 {
    color: black !important;
    -webkit-text-fill-color: black !important;
    background: none !important;
    border-bottom: 2px solid #333;
  }
  
  .story-section {
    border-left: 2px solid #ddd !important;
    margin-bottom: 20px;
    page-break-inside: avoid;
  }
  
  .story-section:hover {
    transform: none !important;
    box-shadow: none !important;
  }
  
  .highlight-box {
    border: 1px solid #333;
    background: #f5f5f5 !important;
    animation: none !important;
  }
  
  .works-grid {
    display: block;
  }
  
  .work-item {
    border: 1px solid #ddd;
    background: white !important;
    margin-bottom: 10px;
    transform: none !important;
  }
  
  .quotes-grid {
    display: block;
  }
  
  .quote-item {
    border-left: 2px solid #333;
    background: white !important;
    transform: none !important;
    margin-bottom: 10px;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  // Console message
  console.log('%c📜 பேரறிஞர் அண்ணாதுரை', 'color: #4b2e1e; font-size: 14px; font-weight: bold;');
  console.log('%cLife and legacy of Perarignar Annadurai', 'color: #666;');
  
  // Add scroll animations for story sections
  const storySections = document.querySelectorAll('.story-section');
  
  storySections.forEach(section => {
    section.classList.add('fade-in');
  });
  
  // Intersection Observer for scroll animations
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        // Add a slight delay between each section for better visual effect
        const delay = Array.from(storySections).indexOf(entry.target) * 50;
        setTimeout(() => {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }, delay);
      }
    });
  }, observerOptions);
  
  storySections.forEach(section => {
    observer.observe(section);
  });
  
  // Enhanced hover effect to container
  const container = document.querySelector('.container');
  if (window.matchMedia("(hover: hover)").matches) {
    container.addEventListener('mouseenter', function() {
      this.style.boxShadow = 
        '0 0 50px rgba(194, 168, 109, 0.3), ' +
        '0 0 100px rgba(110, 123, 91, 0.2) inset, ' +
        '0 0 30px rgba(122, 106, 85, 0.2)';
    });
    
    container.addEventListener('mouseleave', function() {
      this.style.boxShadow = 
        '0 0 40px rgba(0, 0, 0, 0.5), ' +
        '0 0 100px rgba(194, 168, 109, 0.1) inset';
    });
  }
  
  // Create floating particles - reduce number on mobile
  function createFloatingParticles() {
    const particleCount = window.innerWidth < 768 ? 10 : 15;
    const colors = ['#C2A86D', '#6E7B5B', '#8A946E'];
    
    for (let i = 0; i < particleCount; i++) {
      const particle = document.createElement('div');
      particle.className = 'floating-particle';
      particle.style.width = Math.random() * 4 + 2 + 'px';
      particle.style.height = particle.style.width;
      particle.style.left = Math.random() * 100 + 'vw';
      particle.style.top = Math.random() * 100 + 'vh';
      particle.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
      particle.style.opacity = Math.random() * 0.5 + 0.3;
      particle.style.animationDuration = Math.random() * 30 + 20 + 's';
      particle.style.animationDelay = Math.random() * 5 + 's';
      document.body.appendChild(particle);
    }
  }
  
  createFloatingParticles();
  
  // Add page load animation
  document.body.style.opacity = 0;
  window.requestAnimationFrame(() => {
    document.body.style.transition = 'opacity 1.5s ease';
    document.body.style.opacity = 1;
  });
  
  // Add subtle parallax effect to floating elements (only on non-mobile)
  if (window.innerWidth > 768) {
    window.addEventListener('scroll', function() {
      const scrolled = window.pageYOffset;
      const floatingElements = document.querySelectorAll('.floating-element');
      
      floatingElements.forEach((el, index) => {
        const speed = index === 0 ? 0.5 : index === 1 ? 0.3 : 0.7;
        el.style.transform = `translateY(${scrolled * speed * -0.5}px)`;
      });
    });
  }
  
  // Add ripple effect to work items and quote items
  function addRippleEffect(element) {
    element.addEventListener('click', function(e) {
      const rect = this.getBoundingClientRect();
      const size = Math.max(rect.width, rect.height);
      const x = e.clientX - rect.left - size/2;
      const y = e.clientY - rect.top - size/2;
      
      const ripple = document.createElement('span');
      ripple.style.cssText = `
        position: absolute;
        border-radius: 50%;
        background: rgba(110, 123, 91, 0.1);
        transform: scale(0);
        animation: ripple 0.6s linear;
        width: ${size}px;
        height: ${size}px;
        top: ${y}px;
        left: ${x}px;
        pointer-events: none;
        z-index: 1;
      `;
      
      this.style.position = 'relative';
      this.style.overflow = 'hidden';
      this.appendChild(ripple);
      
      setTimeout(() => ripple.remove(), 600);
    });
  }
  
  // Apply ripple effect to all interactive elements
  document.querySelectorAll('.work-item, .quote-item').forEach(addRippleEffect);
  
  // Add CSS for animations
  const style = document.createElement('style');
  style.textContent = `
    @keyframes ripple {
      to {
        transform: scale(4);
        opacity: 0;
      }
    }
    
    .fade-in {
      opacity: 0;
      transform: translateY(20px);
      transition: opacity 0.8s ease, transform 0.8s ease;
    }
    
    .fade-in.visible {
      opacity: 1;
      transform: translateY(0);
    }
  `;
  document.head.appendChild(style);
  
  // Handle window resize
  let resizeTimer;
  window.addEventListener('resize', function() {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(function() {
      // Adjust container width on resize
      const container = document.querySelector('.container');
      if (window.innerWidth < 576) {
        container.style.width = '95%';
      } else if (window.innerWidth < 768) {
        container.style.width = '92%';
      } else {
        container.style.width = '90%';
      }
    }, 250);
  });
  
  // Initialize container width
  const initContainer = document.querySelector('.container');
  if (window.innerWidth < 576) {
    initContainer.style.width = '95%';
  } else if (window.innerWidth < 768) {
    initContainer.style.width = '92%';
  }
  
  // Add keyboard navigation for interactive elements
  document.querySelectorAll('.work-item, .quote-item').forEach((item, index) => {
    item.setAttribute('tabindex', '0');
    item.addEventListener('keydown', function(e) {
      if (e.key === 'Enter' || e.key === ' ') {
        e.preventDefault();
        this.click();
      }
    });
  });
  
  // Add touch feedback for mobile
  document.querySelectorAll('.work-item, .quote-item, .story-section').forEach(item => {
    item.addEventListener('touchstart', function() {
      this.style.transform = 'scale(0.98)';
    }, { passive: true });
    
    item.addEventListener('touchend', function() {
      this.style.transform = '';
    }, { passive: true });
  });
  
  // Smooth scroll to sections when clicked (for better UX)
  storySections.forEach(section => {
    section.addEventListener('click', function() {
      if (window.innerWidth < 768) {
        this.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    });
  });
  
  // Performance optimization
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    document.querySelectorAll('*').forEach(el => {
      el.style.animation = 'none';
      el.style.transition = 'none';
    });
  }
});
</script>

<?php include '../includes/footer.php'; ?>